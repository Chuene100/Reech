//export all constants in this folder:

import icons from "./icons";
import images from "./images";
import { COLORS, SIZES, FONTS, darkMode } from "./theme";
import themeContext from "./themeContext";
import auth from "./auth";
import videos from "./videos";

export {
  icons,
  images,
  videos,
  COLORS,
  SIZES,
  FONTS,
  darkMode,
  themeContext,
  auth,
};
